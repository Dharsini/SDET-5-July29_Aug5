package preprogram1;

/*
 * input string
 * output boolean
 * get the input string and get the length and divide by 2
 * splitting the string using substring method
 * char array to store the vowels
 * for loop to iterate the splitted string character
 * A nested for loop to compare the string vaule with the vowels array
 * if value of char array and vowel matches increase the count
 * repeat for splitted array 2 also 
 * or form a method the counting process
 * return true if the count is equal
 * 
 */

public class HalfaArraysAlike {
	
	public boolean halfArrays(String str) {
		int len = str.length()/2;
		String str1= str.substring(0,len);
		String str2 = str.substring ( len,str.length() );
		
		
		
		
		return false;
		
	}

}
